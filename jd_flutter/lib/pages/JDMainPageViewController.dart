import 'package:flutter/material.dart';

class JDMainPageViewController extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('JDMainPageViewController'),

    );
  }
}